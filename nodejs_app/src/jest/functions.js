const functions = {
    add: (num1, num2) => num1 + num2,
    isNull: () => null,
    checkValue: x => x,
    getUser: () => {
        return {
            name: 'Jerard',
            age: 30
        }
    }
};

module.exports = functions